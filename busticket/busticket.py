# # busticket
# try: s, p, m, n = [int(x) for x in input().split()]
# except: raise Exception('invalid input')
# t_i = [int(x) for x in input().split()]
# if len(t_i) != n:
#     raise Exception("discrepency in number of trips")

# dp = [10**10] * (n+1)

# for i, t_i in enumerate(t_i):
#     cost = [s] * (i+1)
#     cost_s = cost.copy()
#     cost_p = cost.copy()
#     cost_p[i]

#     dp[i+1] = min(, period)